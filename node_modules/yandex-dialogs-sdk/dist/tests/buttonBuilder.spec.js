"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
// const ButtonBuilder = require('../buttonBuilder')
const buttonBuilder_1 = __importDefault(require("../buttonBuilder"));
test('common test for buttonBuilder', () => {
    const expected = {
        title: 'Some button text',
        url: 'https://example.com',
        hide: true,
        payload: {
            some: 'data',
        },
    };
    const button = new buttonBuilder_1.default();
    // test using button builder factory
    button
        .text(expected.title)
        .url(expected.url)
        .shouldHide(expected.hide)
        .payload(expected.payload);
    expect(button.get()).toEqual(expected);
    // test using button builder constructor
    const button2 = new buttonBuilder_1.default(expected);
    expect(button2).toEqual(expected);
});
//# sourceMappingURL=buttonBuilder.spec.js.map