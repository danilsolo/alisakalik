"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const button_1 = __importDefault(require("../button"));
test('create button with string constructor', () => {
    const expected = {
        title: 'send message',
    };
    const btn = button_1.default(expected.title);
    expect(btn).toEqual(expected);
});
test('create button with object constructor', () => {
    const expected = {
        title: 'show messages',
        payload: { test: 'test' },
        hide: true,
    };
    const btn = button_1.default({
        title: expected.title,
        payload: expected.payload,
        hide: expected.hide,
    });
    expect(btn).toEqual(expected);
});
//# sourceMappingURL=button.spec.js.map