import Session from './session';
import ReplyBuilder from './replyBuilder';
import ButtonBuilder from './buttonBuilder';
import { WebhookResponse, WebhookRequest } from './types/webhook';
import { CtxInterface } from './types/ctx';
import { CommandInterface } from './types/command';
import { EventEmitterInterface } from './types/eventEmitter';
import { BigImageCard } from './types/card';
export default class Ctx implements CtxInterface {
    req: WebhookRequest;
    sessionId: string;
    messageId: string;
    userId: string;
    payload: {};
    message: string;
    session: Session;
    eventEmitter: EventEmitterInterface;
    command?: CommandInterface;
    replyBuilder: ReplyBuilder;
    buttonBuilder: ButtonBuilder;
    sendResponse: (response: WebhookResponse) => void;
    enterScene: (sceneName: string) => void;
    leaveScene: () => void;
    constructor(params: any);
    readonly body: any;
    reply(replyMessage: string | {}): Promise<WebhookResponse>;
    replyWithImage(params: string | BigImageCard): Promise<void | WebhookResponse>;
    replyWithGallery(): Promise<void>;
    _createReply(replyMessage: any): WebhookResponse;
    private _sendReply;
}
