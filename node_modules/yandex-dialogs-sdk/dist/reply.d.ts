import { WebhookResponse } from './types/webhook';
interface ParamsType {
    text: string;
    tts?: string;
    shouldEndSession?: boolean;
    endSession?: boolean;
    end_session?: boolean;
    session: {};
    buttons: any[];
}
export declare const reply: (params: ParamsType) => WebhookResponse;
export default reply;
